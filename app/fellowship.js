const opps=['damn', 'gosh', 'shot'];
const total=opps.length;
export { opps,total};

const plus= (a,b)=>{
  return a+b;
}
const huge= (a,b)=>{
  return a*b;
}

// export { plus, huge};
export default huge;